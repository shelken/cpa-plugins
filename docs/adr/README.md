# 架构决策记录

按决策的作用域分两组，编号全仓唯一递增，不因分组重编：

- 根目录：跨插件的仓库级决策（布局、发布流程等）
- `plugins/<plugin-id>/`：单个插件的专属决策（协议基线、清单来源、模型 id 形态等）

新写 ADR 时先判断作用域：改的是仓库怎么运作，放根目录；改的只是某个插件的行为，放该插件目录下。编号取当前全局最大号加一，文件名 `<编号>-<英文短题>.md`。

修订关系用编号互相引用（如 0004 修订 0003 的表述），移动文件不影响。
